import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#03431FFF', // Tea leaf green color
    },
    secondary: {
      main: '#1a5e3a',
    },
    background: {
      default: '#f5f5f5',
    },
  },
});